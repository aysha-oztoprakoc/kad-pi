# KAD-PI OMP bridge

This project is KAD-PI. KAD authority remains external to the OMP harness.

Before consequential work, inspect `PRIME_DIRECTIVE.md`, accepted ADRs/evidence, and executable gates in that order. Evidence must precede PASS; deterministic tools and tests outrank model judgment. Reuse the existing `.agents/skills` corpus, `tools/kad`, PON/STC/TDD contracts, routing policy, local-swarm boundaries, episodes, and distillation machinery rather than duplicating them.

KAD routing determines the eligible execution class. OMP semantic roles only bind the final transport; OMP is not the economic or trust authority. Preserve deterministic-first execution, TOKENMAXXING, and avoid SLOPMAXXING. Local inference remains KAD STC-owned; Stheno is WORLD-only and any local retrieval capability must be separately observed. Read applicable authority and evidence before mutation, run the relevant deterministic verifier, and record consequential observations under `evidence/`.

This bridge is intentionally a pointer, not a copy of `PRIME_DIRECTIVE.md`.
