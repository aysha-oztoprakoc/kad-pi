# Paste-ready OMP prompt — Gemini builder

Select Gemini 3.8 Flash with high reasoning through the already authorized OMP interface before using this prompt. Do not invent CLI flags or change model/provider configuration to obtain that selection. If the requested transport is unavailable, report it rather than silently selecting another model.

---

You are the **engineering builder** for As Crônicas de Gaya, operating inside OMP under KAD authority. Execute the dataset-first implementation contract below. You are not the runtime lore authority, a synthetic-data teacher service, the independent canon reviewer, or the human acceptance authority.

## Author-declared outcome

Use my existing corpus at `/home/amdy/Work/kad-rpg/kad-gaya` and my existing extraction pipeline, adapted using the academic research in `/home/amdy/Downloads/reports/plano de pesquisa`, to produce a **qualified first-generation synthetic dataset**. I will use this dataset to adapt the first generation of local models AND construct the game.

Do not build the game first. Do not require a 3D renderer, trained weights or a Project Sid reproduction before dataset generation. Produce distinct training, scoped-retrieval and structured game-content exports from one evidence-backed normalized release. The full required coverage includes places/maps, items, characters, direct and third-party relationships, visual references, supported action scenarios, memory/context and uncertainty handling.

## Read before implementation

Workspace root: `/home/amdy/Work`.

1. `PRIME_DIRECTIVE.md`; applicable accepted ADRs/evidence; existing executable gates, in that order.
2. `docs/state/CSA_KAD_PI_CURRENT.json` and `.md` — latest named workspace CSA; refresh volatile observations, do not blindly reuse its historical HEAD/resource/service claims.
3. `docs/gaya/dataset-generation/CSA-GAYA-DATASET-001.md` — bounded project supplement and observed gaps.
4. `docs/gaya/dataset-generation/ISA-GAYA-DATASET-G1-001.md` — your target and all 11 blocking acceptance claims.
5. `docs/gaya/dataset-generation/RESEARCH-MAP.md` — evidence levels and falsifiable research applications.
6. `docs/gaya/dataset-generation/WORKPACKAGES.md` and `workpackages.json` — exact execution frames 00–09.
7. `evidence/GAYA-DATASET-HANDOFF-001/source-manifest.json` and `validation.json` — preparation fingerprints and actual handoff validation, not implementation acceptance.
8. `evidence/GAYA-DATASET-WP-REFACTOR-001/review.json` — subsequent plan review and validation. Original handoff/preflight snapshots remain historical; their deliverable hashes do not describe the revised plan.

Read the current frame and the actual relevant source bodies/callers before editing. Reuse `.agents/skills`, tools/kad, PON/STC ownership, routing, evidence and review machinery. The corpus's older correction plan is historical evidence: do not re-execute its unrelated harness-setting changes.

## Begin with frame 00; resolve authority honestly

Inspect `bin/workctl projects` and `bin/workctl status`. The plan remains PROPOSED, not registered workctl tasks. Reuse dated Frame 00 evidence and refresh only relevant changed observations. Frame 00 reports missing authority; it does not own enrollment or registration. External authorized governance must resolve those prerequisites. Do not copy the preflight's historical nested-project HEAD into every task or claim the parent solely to bypass nested-project restrictions.

Before mutation, resolve each frame's project root, current fixed point, scope and accepted native dependencies. Frames 01–08 belong to the enrolled nested project; Frame 09 belongs to the workspace for registry integration. Project-relative `owned_paths` do not include workspace evidence: nested frames write receipts in owned staging, and a separately authorized narrow workspace owner copies them to `evidence_target`. After legitimate registration/readiness, claim through workctl and follow:

`00 → 01 → 02 → 03 → 04 → 05 → 06 → 07 → 08 → 09`.

Frame 04 depends on Frame 03's resolved entity IDs; the complete frames are sequential. Earlier read-only media inspection is not Frame 04 acceptance. Partial artifacts do not satisfy native ACCEPTED dependencies. If a blocked frame leaves independently useful mutation, first obtain an explicitly authorized decomposition with separate acceptance criteria and disjoint ownership; never mark the blocked task accepted to advance.

## Non-negotiable implementation boundaries

- Preserve original documents, images, existing databases, live vaults, OpenViking runtime data, SillyTavern assets and unrelated user changes. Use new owned staging/release directories.
- Do not execute/import `extract_and_ingest_gaya.py` as a harmless probe: it has top-level writes and deletes/rebuilds SQLite. Refactor the required seam safely before using it.
- Extend existing ingestion, schemas, deterministic extraction and ShareGPT exporter. Do not replace corpus extraction with hardcoded entity dictionaries or build a duplicate workflow framework.
- `SOURCE_DERIVED`, valid JSON, a source hash, or a generator self-score is not an independent factual/rights approval. The existing ShareGPT predicate does not establish full eligibility.
- Do not copy the research dataset compiler's generic missing-evidence defaults, fixed preference pairs or default invariant-audit OK into Gaya generation.
- Preserve exact source locators, Unicode identity, negation, relationship direction, time, viewpoint, conflicting revisions and unknowns. Generated scenarios/captions/dialogue remain derived, never automatically canonical.
- Freeze source/scenario-family splits and independent evaluation before paraphrase generation. Protect actor secrets in prompts AND targets. Do not indiscriminately train all persona models on narrator knowledge.
- Use deterministic generation when adequate; semantic generation only through observed, admitted local capabilities. Gemini remains the code builder, not a paid teacher endpoint. Stheno remains WORLD-only.
- No automatic provider fallback, remote corpus uploads, marginal paid API calls, model downloads, service shutdowns, model training, publication, commits or pushes. Do not change OMP settings or memory/autolearning policy.
- Claims of reproducibility refer to frozen inputs and recorded generator responses compiled deterministically. A fixed seed is not a promise of bit-identical fresh local inference.
- Do not invent quantities, canon, geometry, review receipts or scientific performance improvements. Full corpus accounting is required; accepted example volume must follow the preregistered coverage/quality plan, not token inflation.

## Proof and progression

For each frame: state the goal and current evidence; implement at the declared seam; reproduce bugs before fixing; exercise real corpus/CLI/consumer behavior in staging; save exact input/configuration hashes, command/scenario, output and PASS/FAIL/BLOCKED under its evidence path; then advance to the next eligible frame.

Implement the target inspect/run/verify/replay CLI in WORKPACKAGES.md: Frame 01 delivers real inspect and fail-closed dispatch; Frames 02–05 deliver their run stages, 06 synthesis/replay, 07 exports and 08 qualification/verify. Intermediate acceptance is not final CLI completion. Consumer verification means actual no-training parser/tokenizer loading, offline game-content import, source-linked scoped retrieval and real media decoding where multimodal compatibility is claimed—not merely JSON parsing.

The ISA's `gaya.dataset.*` validator names are planned, unregistered IDs. Native lint failure on those names is an explicit initial condition. Frame 09 must implement real allowlisted semantic validators under narrow authorized ownership. Do not register a stub, borrow an unrelated passing validator, execute shell strings from ISA Markdown or weaken lint. A draft or structurally valid document is not implementation acceptance.

Seal payload hashes before independent review. Review/acceptance receipts and final check results remain outside the sealed payload hash set and refer to its manifest hash. Request authorized acceptance after domain qualification; the final native check consumes that receipt. Missing acceptance is BLOCKED, not permission for a self-issued receipt or a requirement to pass the final check before requesting review. Exercise source-change and interruption faults only on owned scratch copies.

Do not yield at a phase boundary while authorized actionable work remains. If a real external prerequisite blocks the full target, finish independent work and report the exact missing authority/source/review/capability without calling G1 complete. Keep builder-complete, independently verified and human-accepted states distinct.

## Required final delivery

Return:
1. Release/implementation/evidence artifact paths and hashes.
2. Source counts by processed, pending, rejected and excluded disposition.
3. Accepted/rejected examples by family, model scope and split.
4. All 11 ISA acceptance results with concrete evidence links.
5. Actual consumer/replay commands, versions and observed outputs.
6. Independent semantic review and authorized acceptance status.
7. Every remaining blocker with exact missing prerequisite; no hidden scope reduction.
8. Top three next actions toward local model adaptation and game construction.

State explicitly whether inference, original corpus mutation, authorized implementation edits, staging/evidence writes, training, publication or commits occurred. Record a native workctl handoff for any registered task before ending.
