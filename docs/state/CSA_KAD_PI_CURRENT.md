# KAD-PI Current State Artifact (CSA)

**Schema**: `kad.csa/v1` · **State**: `CURRENT` · **Generated**: 2026-09-11T15:46:34Z
**Evidence cutoff**: `3a0b5b0f2cb11ed00b65ef8da7bb49c309c42af5`
**Receipts**: `evidence/WP-KAD-MEMORY-SUBSTRATE-057/`

> This is the empirically observed current state, not intent. Machine source: `CSA_KAD_PI_CURRENT.json`. Every fact carries provenance (`state_class` + `evidence`).
> The **pre-refactor baseline** for `WP-KAD-MEMORY-SUBSTRATE-057` is preserved as `CSA_KAD_PI_CURRENT_2026-09-11.{md,json}`.

## Repository
| Field | Value | Class |
|---|---|---|
| HEAD | `3a0b5b0` | VERIFIED_CURRENT |
| Branch | `main` | VERIFIED_CURRENT |
| Remote | `https://github.com/aysha-oztoprakoc/kad-pi.git` (HEAD `2f2ae35`) | VERIFIED_CURRENT |
| Divergence | `1` (ahead, unpushed) | VERIFIED_CURRENT |
| Dirty | `true` (469 paths) | VERIFIED_CURRENT |
| Mirror | `vault/` — 192 markdown, regenerated by `bin/kad-memory publish` | VERIFIED_CURRENT |

## Hosts
- **AMDY**: Ryzen 7 7700 (16 threads), ~14 GiB RAM, Navi 44 RX 9060 XT + Raphael iGPU. VRAM/ROCm `UNKNOWN`.
- **TELL**: `UNKNOWN` (known_hosts alias only).

## Harnesses
- **OMP** `18.1.14` (mise), project `.omp/config.yml`, global `~/.omp/agent/config.yml`. Settings schema source `UNKNOWN` (binary-only install).
- **Project settings**: `memory.backend=mnemopi`, `autolearn=true`, `extendedContext=true`; `enabledModels` now also carries `omniroute/*`. Model roles are unchanged.
- **Memory wiring**: hooks extension `~/.omp/agent/extensions/ai-memory-omp.ts`; MCP entry in `~/.omp/agent/mcp.json` (global, mode 0600). The generated extension resolves the bearer from `AI_MEMORY_AUTH_TOKEN` in the process env — hence the token lives in `~/.omp/agent/.env`. `.omp/mcp.json` deliberately carries **no** ai-memory entry: it is git-tracked and the entry embeds a bearer.
- **Model map**: OMP reads `~/.omp/agent/models.yml`. `bin/omp-kad` is meant to symlink `.omp/models.yml` there, but that path holds a stale regular file, so `bin/kad-models` projects into both.

## KnowledgePlane

### Memory substrate (new)
| Field | Value |
|---|---|
| Backend | **ai-memory 2.1.1** (OKF v0.2, git-backed markdown + SQLite index) |
| Endpoint | `http://0.0.0.0:49374` (loopback + LAN, bearer-authenticated; anonymous → 401) |
| Data dir | `/home/amdy/Work/.ai-memory` (gitignored; owns its own wiki git history) |
| Wiki of record | `.ai-memory/wiki/01a090eb-…/01a090eb-…/` (pointer at `.ai-memory/vault-path`) |
| Vault of record | `true` — Obsidian is bound to the record, not to the mirror |
| Embeddings | **local**, in-process candle/all-MiniLM-L6-v2, 384-dim, no key, no egress |
| LLM provider | **none** (zero-LLM mode) |
| Service | systemd `--user` `ai-memory.service`, enabled, active |
| Volume | 192 live pages · 253 page versions · 11 sessions · 22 observations · 173 embeddings |

**`vault/` is now a deterministic mirror** committed for clones/CI/remote. Every mutating vault command (`ingest`, `propose`, `approve`, `apply`, `rebuild`) **fails closed** against the mirror while the record pointer exists; read-only commands keep working so a fresh clone still validates.

**Retrieval adapter**: `tools/kad/knowledge-plane-adapters.mjs → createAiMemoryAccessAdapter` — `authority: false`, records always `acceptance_state: PROPOSED` with `source_ref` + `source_hash`, and degradation (unreachable/401/missing token) returns `DEGRADED` / `[]` / `null` instead of throwing.

**OpenViking: RETIRED.** Runtime path, adapter module and its test removed; historical references under `evidence/**` and `.agents/work/**` left untouched by design.

## Skills
15 canonical (ISA-KAD-SKILL-ROLE-002) · 39 lockfile · 49 surface dirs · 16 `LOCAL_DELTA`. Plus 5 ai-memory managed skills under `.agents/skills/`.

## Plugins
Marketplace `claude-plugins-official` (catalog only). Extensions `kad-control-plane`, `kad-context-economy`. MCP: `context7` via `npx` (**authority-inversion risk**), `ai-memory` over native HTTP.

## Compute

| Surface | Value | Class |
|---|---|---|
| Local Stheno `:5001` / Qwen `:5002` | `DOWN` (`bin/kad-serve` not running) | VOLATILE_CURRENT |
| Local model registry | `kad-local-model-registry-1`, **19 models** | VERIFIED_CURRENT |
| External provider registry | **17 providers** (16 + the gateway) | VERIFIED_CURRENT |
| **Gateway** | **OmniRoute 3.8.50**, `http://127.0.0.1:20128/v1`, loopback-only, systemd `--user`, active | VERIFIED_CURRENT |
| Gateway exposure | **1610 advertised → 31 exposed** | VERIFIED_CURRENT |

**Gateway is `TRANSPORT_ONLY`**: registering a connection grants reachability, never qualification. Local KAD GGUF endpoints are *proxied*; admission, lifecycle and eviction stay with `bin/kad-serve`.

**Spend containment**: `config/omniroute-exposure.json` (`LOCAL_SUBSCRIPTION_AND_FREE_TIERS_ONLY`) projects only local endpoints, the operator's subscriptions, and free-tier ids. Metered models are excluded from the harness projection, so no fallback chain can generate an unapproved bill. The full advertised set is recorded in the hash-verified `config/omniroute-catalog.json` for auditability.

**Not yet connected**: `agy`/`antigravity` (Google AI Pro — needs interactive OAuth consent) and `opencode-go` (needs the subscription credential). Both lanes are already declared in the exposure policy, so they appear automatically once connected.

## Security
Governance in `tools/kad/governance/`. **Authority-inversion risk**: `context7` MCP via `npx`.

No raw secrets in tracked files: the substrate bearer and the gateway client key live only in mode-0600 operator files (`~/.config/ai-memory/env`, `~/.omniroute/.env`, `~/.omp/agent/.env`).

**The gateway's upstream default binds `0.0.0.0` with no key requirement** — that would have exposed the operator's paid provider credentials to the LAN. Corrected to loopback before any provider was registered. Note that OmniRoute 3.8.50 requires a valid key on `/v1` regardless of `REQUIRE_API_KEY`, so the dashboard-issued key is mandatory.

`sudo` requires a password on `amdy`, so installation used verified prebuilt release artifacts (ai-memory, sha256-checked) and npm under a user prefix instead of AUR.

## Verification
- `node --test docs/state/test/state-artifacts.test.mjs` → **9/9 pass**
- `bin/kad-isa lint all` → 3 ISAs clean; `bin/kad-isa check all` → **33/33 claims PASS** (aesthetic 10/10, compute-fabric 14/14, memory 9/9)
- Capture round trip proven: an OMP session produced `sessions: 1, observations: 3` in the substrate
- `bin/kad-memory publish` regenerates `vault/` deterministically and is gated on OKF conformance
- `bin/kad-models sync` is idempotent (second run: `unchanged`)

## Open items
1. **`bin/kad-wiki rebuild` refuses to run** and `bin/kad-wiki lint` reports **126 errors**, all on 123 pages that had **zero git tracking at the evidence cutoff** — an uncommitted research/Gaya corpus lacking the `kad_id` and epistemic fields the vault schema requires. None was introduced by this workpackage. Remedy is a content decision (bring them to schema conformance, or keep them out of the mirror); the WP deliberately did not mint identifiers or invent epistemic classes.
2. **OMP orchestration preflight is `BLOCKED`** — before *and* after this workpackage: `UNAPPROVED_OR_PAYG_MODEL_SURFACE` (its spend predicate requires every enabled pattern to resolve to a loopback `auth: none` provider, which the subscription-enabled config has never satisfied), plus `MEMORY_NOT_OFF` and `AUTOLEARN_ENABLED`.
3. `tools/kad/test/local-multi-endpoint.test.mjs` **T2–T4** fail identically at the cutoff (`BLOCKED` vs `DEGRADED`).
4. No commit was made; 469 paths remain dirty, largely from concurrent workpackages.

## Unknowns
TELL reachability · GPU VRAM/ROCm · OMP settings schema source · Google AI Pro OAuth importability · OpenCode Go credential source · gateway per-model pricing (the gateway publishes none, so KAD costing stays with the economic router).

## Deviations
Project `.omp/config.yml` aligns with global toggles (was divergent) · dual skill lockfiles · `.dsh-ptc-canary/` placeholder · two OMP binaries · **plaintext HTTP on the LAN with a bearer token** (Caddy TLS is the recorded upgrade path) · gateway requires a key despite `REQUIRE_API_KEY=false` · OMP reads `~/.omp/agent/models.yml` rather than `.omp/models.yml` · the mirror carries the pre-existing non-conformant corpus · preflight `BLOCKED` before and after.
