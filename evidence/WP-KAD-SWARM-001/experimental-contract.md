# WP-KAD-SWARM-001 contract

Execute one bounded, low-risk retrieval request through a semantic controller, deterministic KAD normalization/routing/context compilation, and a real Pi 0.84.3 Qwen worker. Accept only a schema-valid result whose source path and evidence are independently verified. Record observable PON transitions, economics, and a runtime-only distillation episode.

Qwen is retrieval-only and receives a precompiled packet with one source, source hash, bounded excerpt, one-fact limit, and no tools. Stheno remains external WORLD-only. The controller lane must be approved, non-PAYG, and subscription-backed by configuration; unknown quota/price remains UNKNOWN. One repair is the maximum; failed validation degrades.

Activation and teardown are STC-scoped. KAD owns only the temporary Qwen process and receipt. The external Stheno process must remain untouched and healthy.
