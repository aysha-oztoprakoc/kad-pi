# Frozen portability hypotheses

H1 binary portability; H2 `.agents/skills` reuse; H3 thin governance bridge; H4 one thin agent adapter; H5 semantic role binding; H6 safe workflow; H7 telemetry. Scope is limited to v18.0.9 Linux x64, Work-local paths, existing KAD authority, no auth migration, no paid calls, no auto-learning, no global configuration edits.

RED required an absent Work-local binary/config/adapter. Observed RED: all target paths were absent before this WP, and no OMP role bridge existed. The binary was independently verified before execution.
