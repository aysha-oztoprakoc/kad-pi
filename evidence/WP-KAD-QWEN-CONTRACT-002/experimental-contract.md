# WP-KAD-QWEN-CONTRACT-002 experimental contract

Authorized action only: activate STC-owned Qwen via `LocalInferenceCapability`, inspect `/proc/<pid>/cmdline` and `GET /api/v1/model`, record evidence, dispose Qwen, and prove external Stheno survived.

Forbidden and not performed: `/v1/completions`, `/v1/chat/completions`, prompts, benchmarks, controller calls, Stheno inference, configuration changes, model downloads, R2 rerun.
