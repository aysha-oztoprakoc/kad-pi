# WP-KAD-QUOTA-001 Contract

Observe the already-authorized `openai-codex/gpt-5.6-luna` controller surface using only `pi auth check --no-refresh --json`. Do not refresh credentials, send a probe, enable PAYG, or modify provider state.

Normalize only provider-owned quota data. If this surface exposes authentication readiness but no quota/reset dimension, preserve `UNKNOWN` with null values and record the limitation. Feed the normalized observation through the existing economic router; trust, capability, authority, and spend policy remain dominant.
