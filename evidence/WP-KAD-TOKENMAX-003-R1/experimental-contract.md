# Experimental contract

Recover the Qwen output-contract boundary for the unchanged `TOKENMAX-LIVE-001` trust-isolation audit, then perform exactly one live local recovery from the frozen controller/packet boundary.

The formatter may only trim whitespace, remove an allowlisted JSON markdown fence, or extract exactly one complete parseable JSON object from harmless envelope text. It cannot rename keys, invent fields, alter values, choose among multiple objects, repair citations, or grant acceptance.

The existing `schema-source-evidence-validator` remains the sole acceptance authority. Qwen remains retrieval-only, Stheno remains external WORLD-only, economics/routing remain unchanged, and missing cost/quota/token fields remain UNKNOWN/null.
