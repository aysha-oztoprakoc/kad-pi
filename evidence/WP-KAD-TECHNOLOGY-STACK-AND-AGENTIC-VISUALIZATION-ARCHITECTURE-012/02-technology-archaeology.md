# Technology Archaeology Record — WP-KAD-TECHNOLOGY-STACK-AND-AGENTIC-VISUALIZATION-ARCHITECTURE-012

## 1. Projects Analyzed
1. **KAD-PI**: Node.js Native Strict ESM (`.mjs`), zero build step, native `node --test`, pure CSS variables (`interface/kad.css`), canonical Obsidian vault (`vault/`), CLI toolchain (`bin/`). Classification: **`KEEP`**.
2. **Sofia v3**: Client-side visualization dashboard (`dashboard/`, `interface/`), Vanilla ESM, static projection consumption, live status polling with staleness fallback. Classification: **`AUGMENT`**.
3. **KAD Public Website**: Static HTML/ESM (`site/`), fail-closed publication filtering, pure CSS design tokens. Classification: **`KEEP`**.
4. **data_workspace**: Node.js ESM desktop hardware sensor and M900 input mapping adapter (`package.json`, `node --test`). Classification: **`KEEP`**.
5. **technopagan-netrunner**: Cyberdeck terminal desktop interface (Quickshell, Hyprland, Omarchy UI kit). Classification: **`REFERENCE` / `KEEP`**.
6. **DATA_REIN (Historical)**: Monolithic legacy wiki and predecessor notes on external drive. Classification: **`RETIRE`** (Dossier canonicalized in `vault/50_Projects/Legacy/DATA-REIN-Dossier.md`).
