# WP-KAD-FUSION-OMP-ADAPTATION-007A

Decision: thin OMP-native adapter; pinned upstream reference, no copied fork. OMP remains host and child command is headless JSON OMP. Economy is default with two IDs observed from `omp models`; Standard has three; Frontier is optional and explicit. Mutation requires both leases and fails closed otherwise.

Observed verification: `node --test tools/kad/test/fusion.test.mjs` — 2 passed. `bin/kad-fusion setup` is idempotent by deterministic overwrite. No premium model call or Fusion network checkout was performed.
