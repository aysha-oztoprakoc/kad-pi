# Adversarial review

- Trust mismatch: exact trust-domain and capability checks precede economics; WORLD cannot satisfy retrieval.
- Deterministic priority: a matching deterministic tool wins before local or remote.
- Resource boundary: missing, stale/unavailable, or non-fitting local contracts return typed reconsideration and zero inference.
- Validation: local acceptance requires the caller's deterministic validation; failure creates `LOCAL_VALIDATION_FAILED` without remote invocation or retry.
- Escalation: remote execution requires one catalogued reason; authority-sensitive tasks remain `REMOTE_REQUIRED` and are not silently executed.
- Context: packets use selected source hashes and omit parent chat, transcripts, and reasoning; selector duplicates are canonicalized.
- Economics: PAYG is rejected by existing policy; UNKNOWN quota remains null/UNKNOWN; no dollar savings are claimed.
- Capability promotion: one success or failure cannot mutate global capability state; this run had no local episode.
- Lifecycle/compatibility: unavailable Qwen is not resurrected and legacy workers without explicit contracts cannot enter this surface.
- Budget: one local call maximum, zero repair calls, zero remote calls.

No concrete contamination was found beyond a test-fixture correction: trust/capability adversarial cases were made to mismatch both local and remote candidates so the assertion tests the intended authority boundary.
