# Incident and authority baseline

R1 exposed a receipt with `actor.project_lead` while the workctl executor was distinct. R2 confirmed V1 has only `actor_id`, so issuer/delegate separation was not machine-verifiable. R0/R1/R2 experiment evidence remains preserved under `evidence/EXP-KAD-OFFLINE-SURVIVAL-001/`. This workpackage changes governance only; it performs no network mutation or offline experiment execution.
