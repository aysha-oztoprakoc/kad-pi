# V1 expressiveness defect

`HUMAN_AUTHORIZATION_RECEIPT_V1` stores one `actor_id`; it cannot prove issuer-to-delegate identity separation. Free-text notes are not validation. Historical V1 remains unchanged and delegated use requires V2.
