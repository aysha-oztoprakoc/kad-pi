# C5-01 Human-Terminal NM Reapply Rehearsal

Human interaction decision: `DONE — NM REAPPLY EXECUTED`.

Exact command presented:

```text
sudo /usr/bin/nmcli device reapply enp7s0
```

The human entered authentication only in the local terminal. The agent did not request, receive, store, echo, or type a password.

The decision was not treated as evidence by itself. Immediate read-only verification followed and is recorded in `02-nm-reapply-verification.md`.

Classification: invocation is human-reported; postconditions are directly observed by read-only commands.
